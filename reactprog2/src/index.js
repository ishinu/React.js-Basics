// import App from './App';
// import React from 'react';
// import ReactDOM from 'react-dom/client';


// const root = ReactDOM.createRoot(document.getElementById('demo'))

// root.render(
//     <React.StrictMode>
//         <App/>
//     </React.StrictMode>
// );

import React from 'react';
import ReactDOM from 'react-dom/client';

const element = <h1>Single index</h1>;

const root = ReactDOM.createRoot(document.getElementById('demo'));
root.render(element);
